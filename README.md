# Trying preact/compat in Node.js

This example shows error when using `preact/compat` in Node.js

## How to reproduce

```bash
# installing dependencies
npm install

# run example application
npm run dev
```
